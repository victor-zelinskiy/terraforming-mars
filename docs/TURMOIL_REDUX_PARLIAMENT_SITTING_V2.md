# «Заседание v2» — Парламент Turmoil Redux (2026-09-21, одна автономная итерация)

Оценка → концепция → изменения по блокам → контракты → бюджеты → проверка → честные границы. Дизайн заседания:
`docs/TURMOIL_REDUX_PARLIAMENT_ASSEMBLY.md`; реализация Э0–Э4: `docs/TURMOIL_REDUX_PARLIAMENT_SITTING.md`; финальный
прогон и полировка: `docs/TURMOIL_REDUX_PARLIAMENT_FINISH.md`; карта частей после v2:
`docs/claude/console/parliament-sitting.md`; журнал итерации: `docs/claude/parliament-sitting-progress.md`
§ «Заседание v2»; закон: `.claude/rules/console-ui.md` § THE PARLIAMENT SITTING.

## 0. Резюме

Политическая фаза стала **одним проходом с двумя нажатиями A на поколение**: `ВЕРДИКТ ─A─▶ [ПОВЕСТКА → ПОДДЕРЖКА →
ПРИНЯТИЕ] → НАГРАДА (волна | ВЫБОР | ПОЛУЧЕНИЕ | A «К полю» → РАЗМЕЩЕНИЕ → «получено») → [ожидание остальных] → ИТОГИ
─A «Закрыть заседание»─▶ поле`. Сервер ставит ворота сборки **до** изменений (`winner → assembly → agenda → support →
enact → effects → refresh → lobby → adjourn → done`), поэтому всё, что видно на ВЕРДИКТЕ, ещё не произошло; клиент
играет изменения битами директора **из диффа двух видов** — одинаково для собственного ответа и для ответа другого
места, пришедшего опросом / WS. Ни одного wall-clock в дереве Парламента; каждое удержание — именованное, с потолком.

## 1. Оценка — семь проблем и их причины

| # | Симптом | Причина | Закрыто |
| --- | --- | --- | --- |
| 1 | Раскладка голосования прыгает, карты усаживаются во время полёта куба | `billGeometry` включался по факту ввода, а не по реальной оплате; refit пересчитывал карты после A | Б5: геометрия счёта только при реальной `votePayment` и ВНУТРИ фиксированной панели; `fitFrozen` от A до ухода |
| 2 | Пустая рамка workspace перед уходом | `--concluded` гасил тело раньше, чем уходила поверхность | Б5: одна анимация ухода всей поверхности (директор), `flow-complete` в момент посадки |
| 3 | Переход к тайлу победителя — автоматически / поздно | `yieldStackToBoard` по `placementActive` | Б3: явная дверь A «К полю»; подсветка гексов ждёт (`setConsolePlacementHeld`); только нажатие уступает стек |
| 4 | +2 растения «Биокуполов» приходили дельта-чипом | вотчер `rewardsOwed` ждал `stage === 'sitting'`, а стадия была `submitting` | Б3: дверь награды не зависит от переходной стадии — реестр публикует долг, директор берёт по позиции сервера |
| 5 | Повестка уже показывает новый шаг при открытии | ворота стояли ПОСЛЕ изменений (`enact → assembly`) | Б1: ворота ДО изменений; Б2: `agendaAwaits` держит маркер на старом шаге до бита |
| 6 | Правительство пустое / задание уже новое / хаос кубов / партия дублирована | то же + оппозиция и правитель в одном ряду разными экземплярами | Б1 + Б2: `govBefore` / `questBefore` / `rulerBefore`; внизу только пять тайлов оппозиции, плитка правителя — телепорт того же экземпляра в правительство |
| 7 | ОБНОВЛЕНИЕ и ЗАКРЫТИЕ — два экрана | две страницы на один серверный шаг | Б4: одна стадия ИТОГИ — биты обновления, затем карточка «Итоги поколения N» |

## 2. Концепция

- **Сервер** (`ParliamentPhase.drive`): `winner → ▶assembly◀ → agenda → support → enact → effects → refresh → lobby →
  ▶adjourn◀ → done`. Шаги идемпотентны (`applied` / `appliedBySeat`), старый сейв с воротами ПОСЛЕ принятия
  догоняется без повторного применения (`tests/parliament/ParliamentPhase.spec.ts` § legacy save).
- **Игрок**: два нажатия A на поколение. Между ВЕРДИКТОМ и ИТОГАМИ страницы поворачивает **директор** — биты идут
  последовательно, каждый под своим удержанием, ≥ 250 мс друг от друга; A во время бита = «дожать» (никогда не пропуск).
- **Один и тот же посев удержаний** для собственного ответа и для опроса: `seedParliamentSittingHolds(before,
  after)` в том же синхронном блоке, что и применение вида (`gameTransport.seedRewardHolds` и `App.update`).
- **Перезагрузка** садится на шаг сервера в конечных позах: ничего не переигрывается (`sittingSessionKnown`).
- **Крошка**: `ПАРЛАМЕНТ › ЗАСЕДАНИЕ › ВЕРДИКТ | ПРИНЯТИЕ | НАГРАДА | ВЫБОР | ПОЛУЧЕНИЕ | РАЗМЕЩЕНИЕ | ИТОГИ`; B до
  ИТОГОВ = свернуть, на ИТОГАХ = none.

## 3. Изменения по блокам

### Б1 — сервер: ворота до изменений
- `src/server/parliament/ParliamentPhase.ts` — порядок шагов; `assembly` сразу после `winner`; заголовок ворот
  «Заседание поколения N: вердикт». `src/common/parliament/ParliamentTypes.ts` — порядок в union.
- Спеки: `ParliamentPhase.spec.ts` (ворота v2, порядок Повестка → поддержка → принятие, **старый сейв** из
  `tests/parliament/fixtures/legacy-assembly-after-enact.json`), `ParliamentModel.spec.ts`, `ResolutionContract.spec.ts`
  без изменений контракта; `e2eFixturesLoad` зелёный на перегенерированных `parliament-*` (42 фикстуры).

### Б2 — клиент: один проход, удержания из диффа
- `consoleSittingFlow.ts` — четыре стадии (`verdict · enact · reward · results`), страницы шага, авто-страницы,
  `sittingStartPage(position, played, receiptOwed)`, ключи крошки/глагола.
- `parliamentSittingSeed.ts` (новый) — DETECT барьера/обновления → посев `enactment` / `renewal` семейств удержаний
  (`heldSlots`, `winnerSlot`, `agendaAwaits`, `supportIncoming`, `returns`, `govBefore`, `rulerBefore`, `questBefore`,
  `freshFaces`, `hiddenCubes`, `deckPending`, `lobby`); идемпотентно по кадрам одного шага.
- `sittingDirector.ts` — биты ПОВЕСТКА (сегмент, глайд, пульс, тики влияния, бонус) → ПОДДЕРЖКА (партия за партией,
  ярус оппозиции виден целиком) → ПРИНЯТИЕ (старый закон в сброс, победитель FLIP слот → правительство, кубы домой,
  смена правительства, задание RELEASE → UNFOLD → REVEAL); `hurry` («дожать»); reduced — позы сразу.
- `ConsoleParliamentSection.vue` — проход `runWalk` / `planOpening` / `enterServerStep` / `mayLeave` / `endSitting`;
  `ConsoleParliamentGovernment.vue` / `…Parties.vue` — плитка правителя одним экземпляром (Teleport в
  `[data-parl-ruler-slot]`), внизу пять тайлов оппозиции, общие токены размера `--con-parl-tile-w/h`.

### Б3 — награда: дверь, без wall-clock, «К полю»
- `parliamentRewardBeat.ts` — ни одного таймера (гард `parliamentNoTimers.spec`: allow-list только `SUBMIT_SAFETY_MS`);
  выпуск по посадке чипа / явному концу стадии / потолку реестра 35 с с `diagnose`; пауза не выпускает; бонус Повестки
  держится только когда сцена на экране.
- Дверь тайла: плита `[data-sit-door]` + A «К полю» (`ConsoleShell.openParliamentBoardDoor` →
  `consoleParliamentUi.boardDoorOpen`); `parliamentPlacementDoorClosed` держит подсветку гексов; возврат читает
  квитанцию на странице НАГРАДА («получено»), затем проход идёт дальше.

### Б4 — ИТОГИ: одна стадия
- `ConsoleParliamentSitting.vue` — панель `results`: биты обновления над столом, затем REVEAL карточки
  «Итоги поколения N» (принята · победитель голосования · ваша награда · новые резолюции с партиями · в лобби);
  ожидание остальных — в строке заголовка карточки; A «Закрыть заседание» = ворота 2.
- Стадии ОБНОВЛЕНИЕ / ЗАКРЫТИЕ удалены (`sittingBeats.ts` STAGE_ORDER = 4).

### Б5 — голосование без прыжков, уход одной поверхностью
- `ConsoleParliamentVoteMode.vue` — `billGeometry = paymentStands && (paying | submitting)`; refit-вотчер удалён;
  `freezeParliamentFit` от A до ухода; `flow-complete` в момент посадки; `--concluded` удалён.
- `console_parliament_vote.less` — счёт как абсолютный оверлей внутри `.con-parl__info-vote`; ряды карт не меняются.

## 4. Контракты (что обязано оставаться верным)

1. **Ворота стоят до изменений.** На ВЕРДИКТЕ стол — как проголосовали, правительство — предыдущее, Повестка — на
   старом шаге, задание — старое. Гард: `ParliamentPhase.spec` + пробник v2 («nothing early»).
2. **Одна функция посева на оба пути.** `seedParliamentSittingHolds` вызывается из `gameTransport.seedRewardHolds`
   и `App.update` в том же синхронном блоке, что применение вида. Гард: `parliamentSittingSeed.spec` (own submit /
   poll одной таблицей), пробник remote.
3. **Биты — по очереди, под именованным удержанием, с потолком.** `parliament-sitting:<stage>[/<beat>]`, потолок
   гасит клин (гасит полёты, снимает display-holds), никогда не «просто счётчик».
4. **Дверь к полю — только нажатие.** `placementActive` не уступает стек; подсветка гексов ждёт `boardDoorOpen`;
   возврат — квитанция на НАГРАДЕ (`sittingStartPage(…, receiptOwed)` садит проход на страницу награды).
5. **ИТОГИ — одна стадия и один стоп.** Карточка скрыта (`data-sit-results-hidden`) пока идут биты обновления;
   A на ней — ворота 2; B — none.
6. **Геометрия голосования заморожена от A до ухода.** `fitParliamentCards` / ResizeObserver / view-вотчер —
   через одну точку `fitFrozen`; счёт живёт внутри фиксированной панели.
7. **Уход — одно движение всей поверхности.** Никакого отдельного затухания тела; `flow-complete` в момент посадки.
8. **Правительство и оппозиция.** Внизу ровно пять тайлов оппозиции; правитель — та же плитка (один DOM-экземпляр)
   в слоте правительства, того же шасси и размера (один токен `--con-parl-tile-w` = min(колонка ряда, блок правителя);
   ряд строится от токена); смена правительства — FLIP плиток, ряд неподвижен после посадки. **Зум правительства
   заморожен на всё заседание** (`govFrozen`): задание председателя меняется вместе с правительством, а зум решается
   из оставленного им места — пересчёт посреди прохода двигал карту и плитку правителя (36 px на Deck).
9. **Карточка ИТОГОВ помещается в ярус** на 1080 и TV (три колонки, две строки под заголовком); на Deck сцена
   занимает дорожку Повестки **целиком** (собственный допуск сцены) — «полдорожки» под плитой не бывает никогда.

## 5. Бюджеты и замеры (пробник v2, `--workers=1`, 2026-09-21)

| Профиль | Порядок битов после A | Волна после A | Дверь / возврат / ИТОГИ |
| --- | --- | --- | --- |
| 1080 | agenda → support → enact | 6.15–6.22 с (4 прогона) | «К полю» → поле → «получено» → ИТОГИ, один A |
| TV 4K | agenda → support → enact | 7.31–7.43 с (3 прогона) | то же |
| Deck | agenda → support → enact | 6.17–6.24 с (3 прогона) | то же; сцена ИТОГОВ берёт дорожку целиком |
| remote (1080, ответ второго места опросом) | agenda → support → enact | — | смена правительства FLIP, волна +1 M€ производства из механики карты |

- Стабильность обзора (`console-parliament-stability`, три профиля): обход d-pad двигает только курсор (ряд из пяти +
  плитка правителя одной ширины), режим голосования — только выбор, смена правительства — ярусы, слот плитки
  правителя и пять боксов ряда неподвижны после посадки; 9/9.
- Геометрия голосования (`console-parliament-vote-geometry`, 1080 + TV): 0 px изменения размеров карт / панели /
  кнопки от A до ухода, голос из лобби и из резерва.
- Уход (`console-parliament-leave`, 1080): конец голосования, конец заседания, B «свернуть» — от `flow-complete` до
  снятия тело видно или уходит корень целиком; 3/3.
- Тик счётчика — в кадре посадки чипа (−34…+400 мс), никогда до вылета чипа; дельта-чип есть.

## 6. Проверка

- **Юниты по чистым моделям**: `consoleSittingFlow.spec`, `sittingBeats.spec`, `parliamentSittingSeed.spec` (новый),
  `parliamentRewardBeat.spec` (без wall-clock), `parliamentNoTimers.spec`, `parliamentGlossary.spec` (канон «К полю»,
  «Итоги», «Оппозиция»), `ParliamentPhase.spec` / `ParliamentModel.spec`, `consoleTaskRouter.spec` /
  `consoleTaskSummary.spec`, `e2eFixturesLoad.spec`.
- **Новые пробники** (только они и гонялись, `--workers=1`): `console-parliament-sitting-v2` (три профиля),
  `console-parliament-sitting-v2-remote`, `console-parliament-vote-geometry`, `console-parliament-leave`,
  `console-parliament-stability` (ряд из пяти + плитка правителя, смена правительства).
- **Старые e2e переписаны под грамматику v2, но не гонялись** (по §0 задания): `console-parliament-sitting`,
  `console-parliament-sitting-motion`, `console-parliament-sitting-reward`, `console-parliament-gallery`,
  `console-parliament-aquifer`, `console-parliament-biodome`; общий драйвер `parliamentDrive.ts` (`turnTo` в
  семантике v2 — A только на вердикте, ожидание «дошёл или прошёл»; FITS-гард `spills-y` для позы заседания).

## 7. Честные границы

- Галерея и переписанные старые e2e не запускались в этой итерации; их первая зелёная — задача следующего прогона.
- На Deck карточка ИТОГОВ не уместилась в ярус 8.4rem — принят собственный допуск сцены (дорожка Повестки целиком).
- Плита «получено» тайла называет только параметр из записи резолюции (кислород); следствия (температура, океан)
  — в HUD, как и раньше.
- «Дожать» закреплён переписанным (не гонявшимся) motion-спеком; пробник v2 его не проверяет.
- Правила игры не менялись (Повестка раньше наград — неприкосновенна); сервер менял только порядок ворот.
