# `docs/claude/` — reference material extracted from the root `CLAUDE.md`

The root `CLAUDE.md` had grown to ~352k characters and was loaded into **every** session, blowing the 40k budget many times over. On 2026-07-27 it was split:

- **`CLAUDE.md`** — only what must apply in every session (north star, hard prohibitions, commands, architecture skeleton).
- **`.claude/rules/*.md`** — condensed subsystem contracts with narrow `paths:` frontmatter, so a rule loads only when you touch its files.
- **`docs/claude/**`** — this directory: the FULL, verbatim, unedited subsystem write-ups. **Not auto-loaded.** Read one on demand before working on that subsystem.
- **`CLAUDE.md.backup.md`** (repo root) — the complete pre-split original, byte-for-byte.

Nothing was thrown away: every character of the old file is either in `CLAUDE.md`, in a rule, or here.

## Index

| Document | Read it before … |
| --- | --- |
| `north-star-and-goals.md` | questioning what this fork optimises for (console-native product, 4 visual goals) |
| `expansion-adaptation-checklist.md` | **widening scope to a new expansion** — the master to-do + guard worklists + per-module gotchas |
| `architecture-reference.md` | needing the full three-layer / card-system / behavior / deferred-action / DB / testing / i18n / card-centering reference |
| `board-information.md` | touching board facts, placement previews, pay-on-commit or placement cancel |
| `journal.md` | touching the game log side panel, tokens or chips |
| `notifications.md` | touching the floating notification layer, variants, hostile events, reveals |
| `presentation-flow.md` | touching foreground sequencing, leases, MarsBot turn presentation |
| `terraforming-progress-hud.md` | touching the terraforming % HUD or the completion ceremony |
| `energy-heat-conversion.md` | touching the end-of-generation energy→heat transition |
| `modal-inputs-and-metadata-contract.md` | routing a new modal input type or attaching per-card option metadata |
| `action-prompt-audit.md` | writing or editing a card `action()` / `actionPreview()` — the pre-collect contract, the leftover-prompt classes it broke, and the guard that now enforces it |
| `premium-tooltips.md` | the full tooltip rationale + adopter list |
| `start-game-flow.md` | touching the generation-1 orchestration modal |
| `rematch-flow.md` | touching the end-of-game rematch subsystem |
| `desktop-ui/deprecation-and-update-model.md` | the full desktop-freeze rationale + the no-remount/structural-sharing/motion rework |
| `desktop-ui/action-ui-rework-and-philosophy.md` | the dedicated-button migration contract + modal philosophy |
| `desktop-ui/overlay-played-cards.md` | the РАЗЫГРАНО tableau, fit planner, pick-mode |
| `desktop-ui/overlay-effects.md` | the ЭФФЕКТЫ master-detail overlay + per-effect stats |
| `desktop-ui/overlay-actions.md` | the ДЕЙСТВИЯ overlay, per-branch availability, confirm modal |
| `desktop-ui/overlay-victory-points.md` | the VP breakdown overlay + server breakdown model |
| `desktop-ui/overlay-hand-cards.md` | the КАРТЫ В РУКЕ overlay, play/sale/select modes, play preview |
| `cards/premium-card-renderer.md` | the `.pcard` face internals |
| `cards/card-information-model.md` | the generated structured card text |
| `cards/fullscreen-card-viewer.md` | the bounded card browser |
| `cards/card-lore-archive-entry.md` | the fullscreen lore aside |
| `cards/card-action-buttons.md` | the `cab-*` button design system |
| `console/*.md` | the full console contracts: task-summary copy, start-scene summary, leak detector, mandatory gate, **prompt admission** (one response = one demand on the player), VueUse foundation, surface motion, TV profile, performance mode, the final-greenery finale, the payment panel (compact ⇄ expanded + its layout-shift contract), the **Information Workspace** (Y — rail-anchored layout, inspected-player context, seam tokens), the **Workspace Band** (`workspace-band.md` — the ONE decision-surface format right of the always-visible rail: `.con-ws-band()` + the `con-ws` marker), **Planet Focus** (`planet-focus.md` — the main-grid placement stage: arcs/off-Mars recede, the planet expands, the global-param display hold + the post-exit scale beat), the **System Surface** (`system-surface.md` — the ONE chassis behind the settings console and the in-game system menu: category rail, value steppers, the fixed detail strip, the constant-frame layout-shift contract, and the keyboard seam into the Menu overlay), the **Colony Focus Stage** (`colony-focus-stage.md` — the colony workspace's detail scene: the dense centred panel, the RETURN BASE that draws the colony-count↔track rule on the track itself, the SEAT contract that makes the flying marker and build cube the size they land at, the measured anchor ladder, and the two-wave entrance where words arrive after the geometry), the **start-flow card motion** (`start-flow-motion.md` — the Game Start Workspace's physical card language: ACTIVE vs PENDING stage + the four phase orders, per-batch flight ownership (why the summary tail used to teleport), destination prewarm, and `Card3DInner`, the reusable 3D card body), **unavailability reasons** (`unavailability-reasons.md` — the three server reason engines and why standard projects needed a third; the co-located hook rule, structural-blocker-first ordering, aggregate reasons that stay exactly true, and the ban on client-side reason guessing) , the **Draw & Select flow** (`deck-pick-flow.md` — «посмотри N карт колоды, оставь K»: the `deckPickPrompt` server marker attached in the ONE funnel every producer goes through, the surface that outlives its own prompt so the picks can physically reach the hand dock before the rest is cleared, and the fit-engine circularity that a bounded box removes), the **Colony Resolution** (`colony-resolution.md` — the Pluto flow's ONE interaction owner: the `colonyResolutionLive` close gate that spans trade → reveal → owner-bonus cycles → the EMBEDDED mandatory discard (`colonies ⊃ hand`) → the track reset; the remote owner-bonus entry through the mandatory announce; the «СБРОШЕНО» seat the discard tray teleports into; and why «the reveal is empty» is never «the flow is over»), the **self-target of a played-card target step** (`played-target-self.md` — «ИСТОЧНИК · ЭТА КАРТА»: why the card that raised the prompt gets a compact PROXY instead of a second full-size face, the solved-cell width contract that keeps selection from re-flowing the row into a vertical stack, the two-marker connector wired between the real boxes, the three non-competing voices around one card, and the zoom-origin redirection that makes X lift the real hero), the **Milestones/Awards workspace** (`ma-workspace.md` — the North-Star flow `Overview → Hero Detail → Commit → Ceremony inside the workspace → Close`: the emblem as the ONE physical object across all four stages, the ceremony EMBED CLAIM that keeps the global shell from double-presenting an own beat, the pinned `heldView` economy, the pure commit-outcome decision with its payment/raced/stale forks, and the suspended-detail draft behind the task-restore door), the **first-action stage + the placement completion barrier** (`start-first-action.md` — the corporation's mandatory first action as the Game Start Workspace's own conditional last stage: the domain discriminator that retires the standalone modal from the start flow, the seat/briefing/wait grammar, and `startBoardExcursion.ts` — the latch that keeps the workspace hidden until a placement's WHOLE causal chain completes), the **Hydro workspace** (`hydro-workspace.md` — «Гидросеть Марса» as a full North-Star workspace: the track as the permanent protagonist over ONE transformable scene, the pre-select ⁄ post-commit split (embedded reward choice, the nested Viron-parity repeat browser, the shared played-target step), the commit pipeline `moving → resolving → result` driven by the marker gate + the busy falling edge, the embedded deck pick of position 5, the position-9 presented target with its frozen counter, the finish VP ceremony, and the park/restore + reconnect nets), the **Standard Projects workspace** (`standard-projects-workspace.md` — the last menu-shaped surface migrated to North Star: the colony pick / patent sale / alt-resource payment as STEPS inside it, the pay-on-commit target model declared beside each `payAndExecute`, the terminal commit beat with no second confirm and no resource flights, the ONE guarded ending and the two phases that must NOT trigger it, the shared `ActionEffectChip` preview built from the card's own rule sources, the generic `−N` discount capsule, and the `SelectColony.placementContext` serialization gap it uncovered), the **play-card RESULT + «ИЛИ» choice** (`play-card-result.md` — the two result levels and why level 2 is an intrinsic CLUSTER rather than a grid of equal columns; the three-state choice (cursor / answer / commit) with its no-auto-select rule, the commit-gate requirement that makes one input physically unable to select AND play, the `flex-basis: auto` trap that stranded «или» beside the first option, and the container query that turns the pair into a column), **potential availability** (`potential-availability.md` — «мог бы» vs «могу прямо сейчас»: the structured `AvailabilityBlocker` that lets `NOT_YOUR_TURN` block the commit without touching the green count or the colour, the turn-independent server projection (`potentialActions`) behind every wheel number, `min(colonies, fleets)` for trade composed from the live trade action's own payment handlers, and the `playable ⊆ potential` enforcement that keeps a sponsor-discounted card from being dimmed while its own РАЗЫГРАТЬ is live), and the **Strategy Rail** (`strategy-rail.md` — the board home's right Milestones/Awards HUD: the LEFT rail's width-token twin, the icon-first medal grammar (owner enamel gem, ready-vs-offered halos, the leader/chaser duel), the dossier overlay that replaced the in-flow context panel so the board never reflows, the seed-then-diff SEAL machine deferred behind `boardHomeIdle`, and the fixed-box 3/3 recompositions), and the **HUD frame / perimeter shell** (`hud-frame.md` — the full-bleed cockpit hull: ONE `--con-hud-h` height token for the two horizontal rails, the side rails as welded hull members (`--con-rail-outer-w`, safe-inset content), ONE `--con-hud-gap` seam around the stage opening, the `.con-stage-surface()` grammar that makes a workspace a STATE of the central scene rather than a modal, the welded right-edge drawers, the welded hand-dock plate, and the colour-only final-generation marker) |

Sibling docs that already lived outside `CLAUDE.md` and are still current: `docs/CONSOLE_MODE_CONCEPT.md`, `docs/CONSOLE_FOUNDATION.md`, `docs/CONSOLE_SURFACE_MOTION.md`, `docs/DESKTOP_UI_PHILOSOPHY.md`, `docs/DESKTOP_DEPRECATION_AUDIT.md`, `docs/MODAL_INPUTS.md`, `docs/EVENT_STAT_FOUNDATION.md`, `docs/ENDGAME_STORYTELLING.md`, `docs/CHOICE_CONTEXT_AUDIT.md`, `docs/DELAYED_TARGET_AUDIT.md`, `docs/PENDING_ACTION_CANCEL_AUDIT.md`, `docs/SPECIAL_TILE_AUDIT.md`.

## Maintenance rule

**Do not @-import these back into `CLAUDE.md`** — that re-spends the context this split reclaimed. When a subsystem changes, update its `docs/claude/` write-up and, only if a *load-bearing prohibition* changed, the matching `.claude/rules/` file. New status/history notes belong here (or in `docs/claude/archive/`), never in the root file.
