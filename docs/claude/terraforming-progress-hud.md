<!-- Reference material moved out of the root CLAUDE.md (2026-07-27 context-budget reorg).
     NOT auto-loaded. Read on demand when working on this subsystem. Verbatim, unedited. -->

## Terraforming progress HUD + "Terraforming complete" event (both modes)

**One shared math, one shared trigger, two mode-native presentations.** The total terraforming percent counts ONLY Temperature + Oxygen + Oceans — **Venus is NEVER an input** (it is not a base game-end parameter; the game-end VARIANTS that require Venus/Moon are the server's `game.isTerraformed` concern, not display progress). The math lives ONCE in `src/client/components/gameProgress/terraformingProgress.ts` (`terraformingProgress({temperature, oxygenLevel, oceans})` → per-axis 0..1 + `total` + integer `percent` **capped at 99 until `complete`** + `complete`; a GameModel can be passed directly) — consumed by BOTH the desktop `Sidebar.vue` gauge (spine + `planet-overall` % readout; per-axis underlines incl. Venus's own stay local) and the console `ConsoleStatusStrip.vue`. **Console top HUD:** the game-end trio (Temp·O₂·Oceans) is one instrument group `.con-status__terra` — a compact label-less percent + a THIN 3px rail underline (`__terra-rail`/`__terra-fill`, width-transition through `--motion-scale`); Venus sits OUTSIDE the group behind a divider (`__param--venus`), then the generation block. **EVERY global readout plays the premium FLIP-SWAP — `ConsoleFlipValue.vue`** (`.con-flipval` in `console.less`): a one-shot split-flap tick (the old value folds up, the new unrolls in + a thin scanline flash), used by the game-end trio, Venus, the total percent AND the generation. `value` is the numeric truth (drives detection), `text` the formatted readout (`-6°C` / `7/9` / `78%`) so it works for any unit, `accent` (`cyan`/`mint`/`gold`) tints ONLY the one-shot beat. Mounts silently (a reload never replays); a DECREASE (undo / game switch) snaps with no flip. **⚠️ LAYERING (load-bearing): the component sits INSIDE the `.con-status__value` cell, never IS it** — the delta-chip `:has(.metric-feedback-host--…)` hooks put an `animation` shorthand on that cell, so a flip on the SAME element would be clobbered by that higher-specificity rule; nested, the two compose (the cell plays the chip's colour/scale pop, the cards fold inside it). The shine keyframe declares ONLY its 35% peak on purpose — the implicit start/end are the element's own computed `text-shadow`, so a persistent glow (the gold `--final` generation) is restored, never overwritten. **The delta chips stay on temperature/oxygen/oceans/venus/percent** (the sidebar's `globals.*` metric keys + scope `global`; the percent is `globals.terraforming-percent`; the `:has` hooks replay the shared `global-num-transition-*` keyframes) — so those announce twice over. **The GENERATION carries NO chip** (the flip alone is its announcement) **and console mode suppresses the desktop's "new generation" toast entirely** (`NotificationLayer.handleGenerationAndPass` gates `buildGenerationNotification` on `!consoleEnabled` — the HUD tick IS the announcement; desktop keeps the toast). The «ПКЛ.» generation label is VISIBLE in ALL profiles (the handheld profile shows it compact at 11px — an earlier pass hid it and a bare number read as ambiguous). Handheld/large profiles tune sizes in `console.less`.

**The completion event** — `src/client/components/gameProgress/terraformingCelebration.ts` (module reactive; the shared brain): `observeTerraformingProgress(view)` is called from `NotificationLayer.update()` (both modes) with the seed-silently-then-diff pattern (mirrors generation/pass/scale-claims) — reload/reconnect into an already-terraformed game shows ONLY the persistent states, never the cinematic; a live not-complete→complete transition bumps `celebrationNonce` exactly once. **Two separate truths:** `complete` (three params — what the rails show at 100%) vs `finalGeneration` (authoritative last-gen: multiplayer → server `isTerraformed` [variant-aware]; solo → `generation >= lastSoloGeneration`, since solo runs to a fixed generation regardless). The subtitle «Это последнее поколение» is shown only when honest (`celebrationFinal`); otherwise the neutral «Температура, кислород и океаны достигли максимума» (solo mid-run / Venus-required variant — the event still fires on the three params, per the "never wait for Venus" rule). Surfaces: desktop → a prestige `terraforming-complete` notification variant (mint, one-shot glow, TTL 14s, NOT suppressed by an open journal) + `planet-sidebar--terraformed/--celebrating` gold states; console → the desktop card is SUPPRESSED and `ConsoleTerraformingCeremony.vue` (mounted in `ConsoleShell`; replaced the old small top banner) plays the MA-ceremony-grade CENTRE-STAGE cinematic — soft veil + procedural terraformed-Mars hero (pure CSS gradients, no asset) + staged text rises + the shared gsap `ceremonyFx` burst, headline «Марс терраформирован» with the honest claim badge («Текущее поколение станет последним» only when `celebrationFinal`, else the neutral three-param line; ru keys in `ru/console.json`); `pointer-events: none`, bounded lifetime, z 11722 (a nose above the MA ceremony), while the strip pulses off the same nonce. **The shared burst director `src/client/console/ceremony/ceremonyFx.ts`** (ring shockwaves + radial sparks + flash + one light sweep; accents `gold`/`medal`/`terra`; transform/opacity-only, one-shot, `motionMs`, reduced-motion spawns nothing, idempotent `stop()`) is ALSO layered onto `ConsoleMaCeremony` (full burst on the own coronation/seal, a calm ring ping on the remote beat) — when you add a new console prestige beat, reuse it instead of hand-rolling particles. **Persistent final-generation markers:** desktop — the gold `planet-final-gen` chip «ФИНАЛ» under the generation cap; console — the generation block flips to gold «ФИНАЛЬНОЕ ПКЛ.» (`FINAL GEN.` key; re-shown on handheld where the normal label is hidden). **Server unchanged** — the existing idempotent `maybeLogMarsIsTerraformed()` announcement is the journal record (ru translation «Марс терраформирован!» added; note it honours the variant options, so under Venus-required rules the journal line comes later than the client three-param cinematic). Tests: `tests/client/components/gameProgress/terraformingProgress.spec.ts` + `terraformingCelebration.spec.ts` + the builder case in `notificationModel.spec.ts`. **To add a new completion-driven surface:** watch `terraformingCelebrationState.celebrationNonce` (one-shot) / read `.complete`/`.finalGeneration` (persistent) — never re-derive the math locally.

