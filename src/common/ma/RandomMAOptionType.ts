
export enum RandomMAOptionType {
    NONE = 'No randomization',
    LIMITED = 'Limited synergy',
    UNLIMITED = 'Full random',
    // vize1215 fork: one unified pool of EVERY compatible milestone/award (base, fan and
    // modular), no synergy balancing, with clone pairs collapsed to a single chosen variant.
    ALL = 'All milestones and awards',
}
